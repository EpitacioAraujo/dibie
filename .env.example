# Portas expostas no host
WEB_PORT=3000
API_PORT=8000

# Base da API que o browser usa (embutida no build do front).
# Aponte para onde a API está publicada (ex.: http://localhost:8000 em dev).
VITE_API_URL=http://localhost:8000
